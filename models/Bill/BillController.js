const express = require("express");
const Bill = require("./Bill");

const addbill = async (name) => {
  try {
   
  } catch (error) {
    console.error("Error:", error);
    throw error;
  }
};

module.exports = {};
